import { HeaderContent } from "../miniComponents/HeaderContent";
import { Navbar } from "../miniComponents/Navbar";


export const Header = () => {
  return (
    <div>
      <Navbar/>
      <HeaderContent/>
    </div>
  );
}